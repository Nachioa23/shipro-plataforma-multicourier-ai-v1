# SHIPRO 2.0 — variables de entorno (ejemplo). Copiar a .env.local y completar.
# DEUDA 66: Postgres reemplaza SQLite.

# Postgres LOCAL (docker-compose). Puerto 5433 en el host (ver docker-compose.yml).
DATABASE_URL="postgresql://shipro:shipro_local_dev@localhost:5433/shipro_dev?schema=public"

# Postgres PRODUCCION (Linode) — completar en el server, NO commitear el valor real.
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/shipro_prod?schema=public"
